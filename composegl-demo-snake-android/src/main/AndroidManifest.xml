<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application
        android:label="ComposeGL Snake"
        android:hardwareAccelerated="true"
        android:allowBackup="false">

        <!--
            Landscape, because the game is laid out in a 1280x720 design and fitted to whatever
            the window turns out to be. Every configuration change listed here is handled by
            LibGDX itself, so rotating the phone does not restart the game.
        -->
        <activity
            android:name=".AndroidLauncher"
            android:exported="true"
            android:screenOrientation="sensorLandscape"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
